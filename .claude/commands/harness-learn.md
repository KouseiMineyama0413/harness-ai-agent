---
description: 過去のエージェント記録から繰り返す失敗・環境ルールを抽出し CLAUDE.md / プロジェクトノートに反映する
---

<!-- AUTO-GENERATED by `harness skill sync` — edit harness.yaml / project_profile.json instead, this file is overwritten on sync -->

Mine past agent transcripts for recurring failures and environment facts with `harness learn $ARGUMENTS`.
Run `harness learn` first (dry run) to preview the proposed rules; review them for accuracy, then `harness learn --apply` to fold them into the CLAUDE.md managed block and project notes.
